## Test project

Setup steps (via Docker):
- docker compose up --build -d
- docker compose exec app composer run setup
- docker compose exec app composer run dev

Default dev port: 8000
Node version: 24.13.0
PHP version: 8.2
